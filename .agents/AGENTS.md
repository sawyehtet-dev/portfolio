# Project Rules

- **Typography & Punctuation**: Never use em dashes (`—`) in UI text, page titles, headings, metadata, content, comments, or documentation. Use standard hyphens (`-`), en dashes (`–`), colons (`:`), or pipes (`|`) as appropriate.
